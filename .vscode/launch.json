{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9099,
            "pathMappings": {
                "/var/src": "${workspaceFolder}/src",
                "/var/www/html": "${workspaceFolder}/src/dwh"
            },
            "hostname": "0.0.0.0",
            "log": true
        }
    ]
}
